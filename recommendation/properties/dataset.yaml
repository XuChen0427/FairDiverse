{
   item_val: 5,
   user_val: 5,
   group_val: 5,
   group_aggregation_threshold: 10, ##less than such threshold group will be aggregated into one group
   sample_size: 1.0,
   valid_ratio: 0.1,
   test_ratio: 0.1,
   reprocess: False, ##do you need to re-process the dataset according to your personalized requirements
   sample_num: 200,
   history_length: 5,
}